test-oneplus-fajita:
  extends:
    # Extended from build-jobs.yaml.j2
    - .device-boot-flow-separate-artifacts
    - .test-oneplus-fajita
  # This should instead be a .pmos-ci-tron-runner-oneplus-fajita in
  # ci-common that we import here
  # extends:
  #   - .pmos-ci-tron-runner-oneplus-fajita
  tags:
    - cpu:arch:arm64
    - soc:sdm845
    - compatible:oneplus-fajita

  variables:
    CI_TRON_KERNEL_CMDLINE__CONSOLE: "console=ttyMSM0,115200"
